# Vite / React Environment Variables

# The base URL indicating where the Python Flask API is hosted.
# For local development, this defaults tohttp://localhost:5000  in apiService.js if not provided here.
# When deploying to Vercel, set this strictly to your production Render/Heroku API URL.
VITE_API_BASE_URL=http://localhost:5000